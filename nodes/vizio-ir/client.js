/* nestor node web client module (vizio-ir) */

nestor.module({
    init: (exports, resolve) => {
        if (resolve) resolve();
    },
    export: _ => {
        return {
            // exports
        };
    }
});